Sure! Here are the code changes and where to add them:

### 1. Add Placeholder in `popup.vm`
Locate the part of `popup.vm` where you want to display the custom field value and add this placeholder:

```html
<div class="score-display">
    Score: <span id="score-value">Loading...</span>
</div>
```

### 2. Update JavaScript in `popup.vm`
Modify the JavaScript to fetch the custom field value when the page loads:

```html
<script type="text/javascript">
    function initStuff() {
        // Function to extract the issue key
        function getIssueKey() {
            var url = window.location.href;
            var issueKeyPattern = /browse\/([A-Z]+-\d+)/;
            var match = url.match(issueKeyPattern);

            if (match) {
                return match[1];
            }

            var issueKeyElement = AJS.$("#key-val");
            if (issueKeyElement.length) {
                return issueKeyElement.text().trim();
            }

            var selectedIssueKey = AJS.$(".selected-issue-key").text();
            if (selectedIssueKey) {
                return selectedIssueKey.trim();
            }

            return null;
        }

        // Fetch and display the custom field value
        function fetchCustomFieldValue(issueKey) {
            var jiraApiUrl = AJS.params.baseURL + "/rest/api/2/issue/" + issueKey;
            jQuery.ajax({
                url: jiraApiUrl,
                type: 'GET',
                dataType: 'json',
                success: function(response) {
                    var customFieldId = 'customfield_10000'; // Replace with your custom field ID
                    var scoreValue = response.fields[customFieldId];
                    if (scoreValue != null) {
                        AJS.$("#score-value").text(scoreValue);
                    } else {
                        AJS.$("#score-value").text("Not Set");
                    }
                },
                error: function() {
                    AJS.$("#score-value").text("Error fetching data");
                }
            });
        }

        // On page load, fetch and display the custom field value
        jQuery(document).ready(function() {
            var issueKey = getIssueKey();
            if (issueKey) {
                fetchCustomFieldValue(issueKey);
            } else {
                AJS.$("#score-value").text("Issue key not found");
            }
        });
    }

    if (typeof jQuery != "undefined") {
        jQuery(document).ready(function() {
            initStuff();
        });
    }

    document.addEventListener("DOMContentLoaded", function() {
        initStuff();
        JIRA.bind(JIRA.Events.NEW_CONTENT_ADDED, function() {
            initStuff();
        });
    });
</script>
```

### Summary of Changes
1. **Add Placeholder:** Insert the `<div class="score-display">` section with the `id="score-value"` span.
2. **Update JavaScript:** Modify the `initStuff` function to fetch the custom field value on page load and display it in the placeholder.

By making these changes, the custom field value will be fetched and displayed automatically when the Jira issue page loads. Let me know if you need any further assistance!